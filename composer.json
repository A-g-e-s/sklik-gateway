{
    "name": "ages/sklik-gateway",
    "description": "PHP client for the Seznam RS-API (Fénix) – Sklik statistics and Seznam Nákupy",
    "type": "library",
    "require": {
        "php": ">=8.4",
        "guzzlehttp/guzzle": "^7.9",
        "nette/utils": "^4.0"
    },
    "require-dev": {
        "phpstan/phpstan": "^2.0",
        "psr/simple-cache": "^3.0"
    },
    "suggest": {
        "psr/simple-cache": "Provide a PSR-16 cache to persist the access token across requests (recommended)"
    },
    "autoload": {
        "psr-4": {
            "Ages\\SklikGateway\\": "src/"
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "scripts": {
        "phpstan": "phpstan analyse"
    }
}
